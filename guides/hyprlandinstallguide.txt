cd /opt/
sudo git clone https://aur.archlinux.org/yay-git.git
sudo chown -R USER:USER yay-git/
cd yay-git/
makepkg -si
yay -Suy
cd /opt/
sudo git clone https://github.com/soldoestech/hyprland.git
sudo chown -R USER:USER hyprland/
cd hyprland
chmod +x set-hypr
./set-hypr
all questions answer "y"



now you can just
pacman -S hyprland